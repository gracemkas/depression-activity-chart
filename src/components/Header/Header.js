import React from 'react';

const Header = ({ title }) => (
  <div className="instructions">
    <div>
      <h1 className="lead">Depression Activity Chart</h1>
      {/* <h1 className="lead">{ title }</h1> */}
    </div>
  </div>
);

export default Header;
